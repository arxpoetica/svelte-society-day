<IntersectionObserver bind:elem bind:intersecting bind:threshold/>
<div bind:this={elem} class="modal modal-slide-2">
	<div class="svg"><Flamingo/></div>
	<div class="content">
		<h2>A brief history of modality and media</h2>
		<blockquote>
			<strong>Modality, <em>noun</em></strong>
			The classification of the sensory interaction (input/output) between a computer and a human.
		</blockquote>

	</div>
</div>

<script>
	import { story_scroll, view_height } from '@society/js/stores.js'
	import IntersectionObserver from '@society/svelte/IntersectionObserver.svelte'
	import Flamingo from '@society/svelte/svg/flamingo.svelte'
	let elem, intersecting, threshold
</script>

<style type="text/scss">
	.modal-slide-2 {
		position: relative;
		overflow: visible;
		height: 200vh;
	}
	.svg {
		position: absolute;
		bottom: calc(100% - 5rem);
		right: 10%;
		width: 200rem;
		height: 200rem;
		color: $purple;
	}
	.content {
		overflow: hidden;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		position: sticky;
		top: -1rem;
		height: calc(100vh - 99rem);
		width: 100%;
	}
	h2 {
		margin: 0;
	}
	blockquote {
		width: 750rem;
		margin: 0;
		text-transform: none;
		&::before {
			content: '';
			display: block;
			width: 50%;
			height: 4rem;
			margin: 50rem auto;
			background-color: $purple;
		}
		&::after { display: none; }
	}
	strong {
		display: block;
		margin: 0 0 20rem;
		text-transform: uppercase;
		letter-spacing: 3rem;
		font: 40rem/1 Georgia, "Times New Roman", Times, serif;
	}
</style>
